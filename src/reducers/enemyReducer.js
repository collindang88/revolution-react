const INITIAL_STATE = {
  support: 0,
  force: 0,
  blackmail: 0,
  gold: 5
};

const enemyReducer = (state=INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default enemyReducer;